One-sentence: pill status chip in tinted semantic colors.

```jsx
<Badge tone="success">Active</Badge>
<Badge tone="warning">Paused</Badge>
```

- Active/Paused on allowance rules are the canonical uses — keep those two tones fixed.
- Badges are labels, never buttons. Pause/Resume is a Button, not a clickable badge.
