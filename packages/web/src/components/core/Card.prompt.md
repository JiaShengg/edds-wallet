One-sentence: the standard container — white surface, 1px hairline, 22px radius, soft candy shadow.

```jsx
<Card title="Allowance Rules" action={<Button size="sm" variant="quiet">+ New rule</Button>}>…</Card>
```

- Gradient tones (`brand`, `play`) are reserved for kid-facing hero moments. Parent dashboards stay on `surface`/`sunken`.
- Never add a colored left border; the brand does not use accent-bar cards.
