One-sentence: the balance, shown as the biggest number on the screen.

```jsx
<BalanceCard label="My balance" amount="$42.50" caption="Only a grown-up can add or take away money." />
<BalanceCard tone="surface" size="md" label="Edd's balance" amount="$42.50" />
```

- Kid view: gradient `brand` tone, `size="lg"`, 64px Fredoka figure.
- Parent dashboard: `tone="surface"` `size="md"` — dense parent screens stay restrained (PRD §6.1 usage note).
