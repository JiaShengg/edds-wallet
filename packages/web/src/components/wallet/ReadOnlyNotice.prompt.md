One-sentence: the persistent capsule on the child's view explaining that only a grown-up changes the money.

```jsx
<ReadOnlyNotice />
```

- Always present in child mode; it is reassurance, not an error, so it stays purple-tinted rather than red.
