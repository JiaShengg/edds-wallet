One-sentence: the Happy Piggy squircle plus the product name — the app's header lockup.

```jsx
<BrandMark size={44} />
```

- Uses `assets/logo.svg`; when embedding elsewhere, copy that file and adjust the `src` path.
- Never recolor or redraw the piggy. The gradient is the brand's only gradient identity.
